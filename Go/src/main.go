package main

import (
	"bufio"
	"fmt"
	"os"
)

func main() {
	reader := bufio.NewReader(os.Stdin)
	for r := 0; r < 10; r++ {
		for c := 0; c < 10; c++ {
			writer
		}
	}

	writer := bufio.NewWriter(os.Stdout)
	fmt.Fprintf(writer)
}
